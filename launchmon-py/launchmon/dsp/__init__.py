"""Reference DSP for the radar channel.

This is the implementation the Swift port is held to. Every function takes its
parameters explicitly and holds no state between calls, so that replay can
re-run an archived capture under different settings.
"""
