node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
.env
.env.local
.vpic-cache/

# The repository ignores models/ folders (downloaded ML weights); the site's car
# models are part of the site and are committed.
!public/models/
