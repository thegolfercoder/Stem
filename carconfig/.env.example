# Copy to .env.local and fill in. Never commit .env.local.
#
# The MVP runs entirely without these: the catalog is served from seed modules
# and builds are saved in the browser. Setting DATABASE_URL switches the
# catalog and build store over to Postgres once that adapter is implemented.
DATABASE_URL=
