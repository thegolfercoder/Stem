# Core simulation & analysis
numpy>=1.24
matplotlib>=3.7

# Real-time 2D visualization
pygame>=2.5

# Testing (development)
pytest>=7.0

# --- Optional / future integrations -----------------------------------------
# neat-python>=0.92     # topology-evolving neural networks (see behavior/neural.py)
# Blender is used via its bundled Python; no pip install required.
