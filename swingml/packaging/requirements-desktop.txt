# What the packaged desktop application needs at run time.
#
# No PyTorch: the swing model runs as swingml.model.numpy_net from
# swingml/data/swing_event_net.npz. No SciPy: its one use was erf, now written
# out. No separate OpenCV: MediaPipe brings opencv-contrib-python, which provides
# the same cv2, and a second copy was 90 MB of duplicate libraries.
numpy==2.4.6
mediapipe==1.0.1
pydantic==2.13.5
flask==3.1.3
pywebview>=5.0
pyinstaller>=6.10
